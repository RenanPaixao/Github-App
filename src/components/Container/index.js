import ContentPageWrapperStyle from './style';

function ContentPageWrapper({ children }) {
  return <ContentPageWrapperStyle>{children}</ContentPageWrapperStyle>;
}

export default ContentPageWrapper;
